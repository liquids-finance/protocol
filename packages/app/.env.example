# WalletConnect Cloud project ID. A demo project is hardcoded in
# `lib/wagmi.ts`; override it here per deployment (e.g. preview branches).
# Get one at https://cloud.walletconnect.com.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
